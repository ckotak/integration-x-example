import React from 'react'
import PropTypes from 'prop-types'

const ComponentX = (props) => {
    return <div>Hi I am integration X component</div>
}

ComponentX.propTypes = {}

export default ComponentX
